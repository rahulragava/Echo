<UserControl
    x:Class="SocialMediaApplication.Presenter.View.UserListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialMediaApplication.Presenter.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dataType="using:SocialMediaApplication.Models.EntityModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <ListView x:Name="userList" ItemsSource="{x:Bind GetUserDetailViewModel.UserList,Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dataType:User">
                    <StackPanel Orientation="Horizontal">
                        <PersonPicture Width="30"></PersonPicture>
                        <TextBlock Text="{x:Bind UserName,Mode=OneWay}" Margin="10 0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <FontIcon Grid.Row="0"
                  Grid.Column="0"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Visibility="Collapsed"
                  x:Name="NoUserFont"
                  Foreground="DarkGray"
                  Glyph=""
                  FontSize="20" />
    </Grid>
</UserControl>
