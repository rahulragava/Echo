<UserControl
    x:Class="SocialMediaApplication.Presenter.View.PollChoiceUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialMediaApplication.Presenter.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <!--<StackPanel 
            Orientation="Horizontal">-->
           
        <ProgressBar Height="25" 
                     Minimum="0" 
                     Maximum="100" 
                     Value="{x:Bind PostControlViewModel.GetCount(ChoiceSelectedUserList.Count,TotalVotes),Mode=OneWay}" 
                     Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"
                     CornerRadius="5"
                     Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                    
        </ProgressBar>
        <TextBlock FontSize="18"
                   Padding="10"
                   Margin="5 5 5 5"
                   Text="{x:Bind Choice,Mode=OneWay}"/>
        <!--</StackPanel>-->

    </Grid>
</UserControl>
